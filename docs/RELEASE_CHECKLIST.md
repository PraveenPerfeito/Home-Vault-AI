# Home Vault — Release Checklist

**Version:** 1.0.0 (MVP)  
**Target:** Google Play Store — Internal Testing Track  
**Last updated:** Phase 6.2

---

## Phase 6 Release Gate

Before submitting to the Play Store, every item below must be checked.

---

## 1. Firebase Configuration

- [ ] **Firebase project created** — `home-vault` project exists in Firebase Console
- [ ] **Authentication providers enabled:**
  - [ ] Email / Password
  - [ ] Google Sign-In (SHA-1 fingerprint uploaded for release keystore)
  - [ ] Anonymous
- [ ] **Firestore database created** (production mode, not test mode)
- [ ] **Firestore rules deployed** — `firebase deploy --only firestore:rules`
  - Rules file: `firestore.rules` (committed to repo)
  - Verify: users can only read/write their own subcollection
- [ ] **Firebase Storage configured** (for Phase 4 photo upload — bucket created)
- [ ] **Firebase Cloud Messaging** — FCM sender ID visible in project settings
- [ ] **`google-services.json`** — downloaded from Firebase Console, placed in `android/app/`
  - File is gitignored; never committed
- [ ] **`lib/firebase_options.dart`** — generated by FlutterFire CLI, gitignored
  - Run: `flutterfire configure` with release SHA-1

---

## 2. Android Build Configuration

- [x] **Package name** matches Firebase: `com.viyalabs.home_vault`
- [x] **Version code + name** set in `pubspec.yaml`: `version: 1.0.0+1`
- [x] **minSdkVersion** — `flutter.minSdkVersion` (= 21, Android 5.0) ✅
- [ ] **Release keystore** generated and stored securely (NOT in repo) — see `docs/ANDROID_SIGNING.md`
- [ ] **Signing config** updated in `android/app/build.gradle` (replace debug signing) — see `docs/ANDROID_SIGNING.md` Step 3
- [x] **ProGuard / R8** rules enhanced — Phase 6.2 (`android/app/proguard-rules.pro`)
- [x] **ML Kit model pre-download** declared in `AndroidManifest.xml` (`com.google.mlkit.vision.DEPENDENCIES = ocr`)
- [ ] **Release AAB** builds successfully: `flutter build appbundle --release`

---

## 3. App Icons ✅ Complete (Phase 6.1)

- [x] **Launcher icon** — all densities generated (`dart run flutter_launcher_icons`):
  - `mipmap-mdpi` (48×48)
  - `mipmap-hdpi` (72×72)
  - `mipmap-xhdpi` (96×96)
  - `mipmap-xxhdpi` (144×144)
  - `mipmap-xxxhdpi` (192×192)
- [x] **Adaptive icon** — foreground + background layers for Android 8+
  - `drawable-{density}/ic_launcher_foreground.png` + `mipmap-anydpi-v26/ic_launcher.xml`
  - Background color `#3D52D5` in `values/colors.xml`
- [ ] **Notification icon** — monochrome (white); not yet added (no FCM; local notifications use app icon by default)
- [x] **Splash screen** — branded `#3D52D5` background (`drawable/launch_background.xml`)
- [ ] **Play Store icon** — 512×512 PNG required for Play Console upload (use `assets/icon/icon.png` upscaled or regenerated)

Source: `assets/icon/icon.png` (1024×1024). Regenerate: `dart run flutter_launcher_icons`

---

## 4. Privacy Policy

All content requirements are documented in `docs/PRIVACY_POLICY_REQUIREMENTS.md`.

- [ ] **Privacy policy document** written (use `docs/PRIVACY_POLICY_REQUIREMENTS.md` as source)
- [ ] **Privacy policy URL** — hosted at a stable HTTPS URL (GitHub Pages or Firebase Hosting)
- [ ] **In-app link** — accessible from Login screen or Notification Settings
- [ ] **Content covers** (see `docs/PRIVACY_POLICY_REQUIREMENTS.md` for full detail):
  - What data is collected (account data, inventory items, OCR on-device, notifications on-device)
  - Firebase (Google Cloud) as the storage provider
  - Camera + gallery access for OCR label scanning
  - Deletion process: email `praveen@blindmatrix.com`
  - Children's privacy (13+)

---

## 5. Permissions ✅ Complete (Phase 6.2)

- [x] **Camera permission** declared in `AndroidManifest.xml`
- [x] **Camera permission** requested at runtime (handled by `image_picker`)
- [x] **READ_MEDIA_IMAGES** permission (Android 13+) — added Phase 6.2 for gallery access
- [x] **POST_NOTIFICATIONS** permission (Android 13+) — for local expiry notifications
- [x] **SCHEDULE_EXACT_ALARM** (Android 12+) — for exact-time notification scheduling
- [x] **RECEIVE_BOOT_COMPLETED** — reschedule notifications after reboot
- [x] **INTERNET** permission declared
- [ ] Permission rationale dialog shown if camera denied (low priority — scanner still works with gallery)

---

## 6. Play Store Assets

- [ ] **App title:** "Home Vault"
- [ ] **Short description** (80 chars max):
  > Track expiry dates, warranties & home inventory — scan product labels with OCR.
- [ ] **Full description** (4000 chars max) — written and reviewed
- [ ] **Feature graphic** (1024×500 px)
- [ ] **Screenshots** — at least 2, up to 8:
  - [ ] Login screen
  - [ ] Dashboard (with items)
  - [ ] Add Item screen
  - [ ] Scanner screen (scan result)
- [ ] **App icon** (512×512 px, PNG, no alpha for Play Store)
- [ ] **Content rating** questionnaire completed
- [ ] **Category:** Productivity or Lifestyle

---

## 7. Internal Testing Track

- [ ] **Internal testing track** created in Play Console
- [ ] **AAB uploaded** (app bundle, not APK)
- [ ] **Testers invited** (min 1 — developer's own Google account)
- [ ] **Opt-in URL shared** with testers
- [ ] **Test accounts** set up for all auth flows (email, Google, guest)

---

## 8. Pre-Release Validation

Run the Release Candidate Test Plan ([RELEASE_CANDIDATE_TEST_PLAN.md](RELEASE_CANDIDATE_TEST_PLAN.md)) on:

- [ ] **Physical Android device** (API 30+)
- [ ] **Physical Android device** (API 21–29) — for minSdk compatibility

Minimum passing criteria:
- [ ] All authentication flows work
- [ ] Add / Edit / Delete items work
- [ ] OCR scanning works on at least 2 product types
- [ ] App does not crash on any tested flow
- [ ] No data from one user visible to another

---

## 9. Crash Reporting / Monitoring (Optional for MVP)

- [ ] **Firebase Crashlytics** integrated (optional but recommended)
  - Add `firebase_crashlytics` to `pubspec.yaml`
  - Initialize in `main.dart` with `FlutterError.onError`
- [ ] **Firebase Analytics** (optional)
  - Useful for tracking OCR usage + item scan counts

---

## 10. Known Deferred Items (Not Blocking MVP)

| Item | Deferred to |
|------|-------------|
| Photo capture + Firebase Storage upload | Phase 4 |
| Warranty date tracking | Phase 4 |
| Notifications (FCM push) | Phase 5 |
| 2-digit year OCR support (`MM/YY`) | Phase 4 |
| Month-name + slash separator (`JUN/2027`) | Phase 4 |
| `intl` package integration for date formatting | Phase 4 |
| iOS support | Post-MVP |

---

## Release Sign-Off

```
Checklist reviewed by: ___________________
Date: ___________________
APK / AAB version: 1.0.0+1
Firebase project: ___________________
Play Console app ID: ___________________

[ ] All blocking items above checked
[ ] Real device test: PASS
[ ] No known CRITICAL issues

Approved: YES / NO
Notes:
```
